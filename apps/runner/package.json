{
  "name": "@tle/runner",
  "version": "0.0.1",
  "private": true,
  "description": "The Last Exam - Code execution runner service",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@tle/contracts": "workspace:*",
    "@tle/content": "workspace:*"
  }
}
