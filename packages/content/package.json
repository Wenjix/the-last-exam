{
  "name": "@tle/content",
  "version": "0.0.1",
  "description": "Challenge, hazard, tool, and trait content loaders and data",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@tle/contracts": "workspace:*",
    "seedrandom": "^3.0.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "@tle/game-core": "workspace:*"
  }
}
