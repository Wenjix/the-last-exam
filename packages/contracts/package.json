{
  "name": "@tle/contracts",
  "version": "0.0.1",
  "description": "Shared API/WS schemas, types, and runtime validators",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
