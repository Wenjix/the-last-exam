{
  "name": "@tle/game-core",
  "version": "0.0.1",
  "description": "Deterministic game rules, FSM, scoring, and match logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@tle/contracts": "workspace:*"
  }
}
